/* ==========================================================================
   index.js — homepage only
   The wall of rooms is currently pure CSS: hover, lift and the "Enter" chip
   are all handled in css/index.css. This file is the hook for homepage
   behaviour that needs scripting later — a filter, a lazy-loaded tile image,
   an intro animation.
   ========================================================================== */

(function () {
  'use strict';

  /* no homepage-specific behaviour yet */
})();
